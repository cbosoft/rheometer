#include <stdlib.h>

#include "main.h"

int config_main(int argc, const char **argv)
{
  (void) argc;
  (void) argv;

  // TODO: struct config_data *cd = default_config_data();
  // TODO: parse_config_args(argc, argv, cd);

  // TODO: add/edit/remove control schemes, calibrations, schedules

  // TODO: free_config_data(cd);
  return 0;
}
